Enter plan mode and let's plan the following implementation.

If I ask you to load skills, load them to your context without subagents and by using the Skill tool BEFORE ANYTHING. Then, you can use subagents.

This plan will be executed by an agent with a clean context. Ensure you include EVERYTHING the agent needs to know to execute it without checking the references at all. But for safety, also include ALL references in the plan.